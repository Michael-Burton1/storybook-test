// Button.stories.ts | Button.stories.tsx

import React from 'react';

import { Meta } from '@storybook/react'; // story instead of Meta

import { Button, ButtonProps } from './Button'; // Had ButtonProps

import './buttonExample.css'

export default {
  title: 'Components/Button',
  component: Button,
} as Meta;
